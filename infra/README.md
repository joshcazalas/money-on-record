# Infrastructure

AWS infrastructure will live here after the first application architecture is
selected. See [`../docs/engineering/delivery.md`](../docs/engineering/delivery.md)
for the agreed state, environment, validation, preview, and release design.

No placeholder Terraform root is committed: `terraform validate`, provider
locks, plans, and module-version pins become meaningful only when real resources
and providers are declared.
